<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="vit_invoice_receipt.list">
      <field name="name">vit_invoice_receipt list</field>
      <field name="model">vit_invoice_receipt.vit_invoice_receipt</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="vit_invoice_receipt.action_window">
      <field name="name">vit_invoice_receipt window</field>
      <field name="res_model">vit_invoice_receipt.vit_invoice_receipt</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="vit_invoice_receipt.action_server">
      <field name="name">vit_invoice_receipt server</field>
      <field name="model_id" ref="model_vit_invoice_receipt_vit_invoice_receipt"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="vit_invoice_receipt" id="vit_invoice_receipt.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="vit_invoice_receipt.menu_1" parent="vit_invoice_receipt.menu_root"/>
    <menuitem name="Menu 2" id="vit_invoice_receipt.menu_2" parent="vit_invoice_receipt.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="vit_invoice_receipt.menu_1_list" parent="vit_invoice_receipt.menu_1"
              action="vit_invoice_receipt.action_window"/>
    <menuitem name="Server to list" id="vit_invoice_receipt" parent="vit_invoice_receipt.menu_2"
              action="vit_invoice_receipt.action_server"/>
    -->
  </data>
</odoo>